{
  "name": "rgou-ai-worker",
  "version": "1.0.0",
  "description": "Royal Game of Ur AI Worker powered by Zig and TypeScript",
  "main": "src/index.ts",
  "scripts": {
    "build:wasm": "zig build -Doptimize=ReleaseSmall && cp zig-out/bin/ai.wasm src/",
    "build": "npm run build:wasm && echo 'Worker built via wrangler'",
    "dev": "npm run build:wasm && wrangler dev",
    "deploy": "npm run build:wasm && wrangler deploy",
    "deploy:dev": "npm run build:wasm && wrangler deploy --env development",
    "deploy:staging": "npm run build:wasm && wrangler deploy --env staging",
    "tail": "wrangler tail",
    "test": "echo \"No tests yet\" && exit 0",
    "clean": "rm -rf dist zig-out .zig-cache *.a *.o src/ai.wasm"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "royal-game-of-ur",
    "ai",
    "zig",
    "typescript"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.20.5"
  }
}
